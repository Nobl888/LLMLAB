# CI evidence artifacts (generated locally and in GitHub Actions)
.llmlab_artifacts/

# --- Proprietary research/control-layer telemetry (do not commit) ---
topology_summary*.json
summary_by_temperature*.csv
summary_by_constraint_profile.csv
summary_by_temperature_and_family*.csv
runtime_anomalies.csv
family_distribution_by_temperature.png
runtime_vs_temperature.png
profile_comparison_runtime.png

# Local run outputs / evidence packs (generated by experiment runners)
results/
outputs/

# --- Workspace hygiene (this repo lives at $HOME) ---
# Prevent Source Control noise from local machine/environment files.

# Python virtual environments
venv/
autogen-env/
test-env/
**/__pycache__/
*.pyc
*.pyo
*.py[cod]
*.pyd
.Python

# VS Code remote/server
.vscode-server/
.vscode-remote-containers/
.vscode/

# OS / shell / local secrets
.env
.envrc
.secrets
.bash_history
.python_history
.lesshst
.wget-hsts
.motd_shown
.sudo_as_admin_successful

# Common caches
.cache/
.config/
.local/
.npm/
.nvm/
.ollama/
.dotnet/
.docker/

# Cloud CLI folders (local machine state)
.aws/
.azure/

# Some CLIs create single files at $HOME
.aws
.azure

# Autogen/AI tool state
.autogenstudio/

# Other common home-dotfiles (not repo content)
.bashrc
.bash_profile
.bash_logout
.bash_aliases
.profile
.landscape/

# Large unrelated folders that commonly exist in $HOME
projects/
moodenv-backup/
holistic-mood-booster/
